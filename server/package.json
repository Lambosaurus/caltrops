{
  "name": "caltrops-server",
  "version": "1.0.0",
  "description": "v2 server for caltrops api",
  "main": "src/index.js",
  "scripts": {
    "build": "npx esbuild src/index.js --bundle --platform=node --target=node24 --outfile=dist/index.js --minify",
    "package": "npm run build && cd dist && zip -r ../dist.zip .",
    "deploy": "npm run package && aws lambda update-function-code --function-name caltrops-lambda-v2 --zip-file fileb://dist.zip --no-cli-pager"
  },
  "author": "Tim Lamborn",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1057.0",
    "@aws-sdk/client-ses": "^3.1057.0",
    "@aws-sdk/lib-dynamodb": "^3.1057.0"
  }
}
